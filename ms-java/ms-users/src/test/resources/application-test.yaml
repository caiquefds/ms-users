spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:card_block
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
